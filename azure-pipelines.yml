trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

variables:
  - group: kle-secrets
  - name: nodeVersion
    value: '24.x'

steps:
  - task: NodeTool@0
    displayName: 'Use Node.js $(nodeVersion)'
    inputs:
      versionSpec: '$(nodeVersion)'

  - script: npm ci
    displayName: 'Install dependencies'

  - script: npm run build:secure
    displayName: 'Build secure production bundle'
    env:
      VITE_API_BASE_URL: $(VITE_API_BASE_URL)

  - task: AzureStaticWebApp@0
    displayName: 'Deploy frontend to Azure Static Web Apps'
    inputs:
      app_location: '/dist'
      output_location: ''
      skip_app_build: true
      skip_api_build: true
      azure_static_web_apps_api_token: $(AZURE_STATIC_WEB_APPS_API_TOKEN)
<<<<<<< HEAD

  - task: ArchiveFiles@2
    displayName: 'Package backend'
    inputs:
      rootFolderOrFile: '$(System.DefaultWorkingDirectory)/backend'
      includeRootFolder: false
      archiveType: 'zip'
      archiveFile: '$(Build.ArtifactStagingDirectory)/kle-backend.zip'
      replaceExistingArchive: true

  - task: AzureWebApp@1
    displayName: 'Deploy backend to Azure App Service'
    inputs:
      azureSubscription: 'kle-backend-connection'
      appType: 'webAppLinux'
      appName: 'kle-backend'
      package: '$(Build.ArtifactStagingDirectory)/kle-backend.zip'
      startUpCommand: 'node server.mjs'
=======
>>>>>>> 136b87d (Actualización de la aplicación)
